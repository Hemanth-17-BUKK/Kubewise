# AWS region used for STS and EKS calls.
AWS_REGION=us-east-1

# Name used when the service assumes a customer IAM role.
ROLE_SESSION_NAME=kubewise-session

# Provide AWS credentials through your AWS profile, IAM role, or standard AWS
# environment variables. Never commit real credentials to .env.
